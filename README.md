# 👶 تطبيق "صحة نونو" (Seha Nono) - (Flutter)

[Image of Seha Nono App Icon]

مساعد الأم الذكي لمتابعة صحة ونمو الطفل، مصمم للعمل **أونلاين وأوفلاين (Offline-First)**.

هذا هو تطبيق الموبايل (الواجهة الأمامية - Frontend) لمشروع "صحة نونو"، وهو تطبيق شامل مبني باستخدام Flutter لمساعدة الأمهات على تتبع كل ما يخص أطفالهن بكل سهولة، حتى مع انقطاع الإنترنت.

---

## ✨ الميزات الرئيسية (Features)

يتميز التطبيق بمجموعة متكاملة من الأدوات التي تدعم الأم في رحلتها:

* **🌐 أوفلاين أولاً (Offline-First):**
    * **التخزين المحلي (Caching):** يستخدم التطبيق قاعدة بيانات `Hive` لتخزين نسخة محلية من جميع البيانات (الأطفال، التطعيمات، المقالات، إلخ)، مما يضمن **سرعة فائقة** في التصفح والوصول للبيانات حتى بدون إنترنت.
    * **المزامنة التلقائية (Sync Queue):** يمكن للأم إضافة أو تعديل أي بيانات (مثل إضافة طفل جديد، تحديث حالة تطعيم، تسجيل قياس نمو) **حتى لو كان الإنترنت مفصولاً**. يتم حفظ هذه التغييرات في "طابور مزامنة" (Sync Queue) محلي، ويقوم التطبيق برفعها تلقائيًا للسيرفر فور عودة الاتصال بالإنترنت.

* **🔐 نظام مستخدمين كامل:**
    * إنشاء حساب جديد وتسجيل الدخول (يدعم JWT).
    * إدارة الملف الشخصي (تعديل البيانات، حذف الحساب).

* **👶 إدارة الأطفال:**
    * إضافة، تعديل، وحذف بيانات الأطفال المرتبطين بحساب الأم.

* **💉 متابعة التطعيمات:**
    * عرض جدول التطعيمات المجدول **تلقائيًا** لكل طفل بناءً على تاريخ ميلاده (يتم جلبه من السيرفر).
    * تحديث حالة التطعيمات (تم أخذه / قادم) أونلاين وأوفلاين.
    * استقبال **إشعارات تذكير (Push Notifications)** قبل مواعيد التطعيمات المستحقة (عبر FCM).

* **📈 تتبع النمو (معايير WHO):**
    * تسجيل قياسات (الوزن، الطول، محيط الرأس).
    * عرض **تحليل فوري** للقياسات ومقارنتها بمنحنيات النمو القياسية (Z-Scores) الخاصة بمنظمة الصحة العالمية (WHO) لعمر الطفل ونوعه.

* **💬 كوميونيتي (مجتمع الأمهات):**
    * نظام متكامل لإنشاء بوستات (أسئلة ومشاركات).
    * الرد على البوستات (كومنتات).
    * التفاعل مع البوستات (إعجاب / إلغاء إعجاب).

* **🤖 شات بوت ذكي (مساعد الأم):**
    * نظام رد آلي يعتمد على **البحث الذكي (Atlas Search)** في قاعدة بيانات موثوقة (FAQs) لفهم سؤال الأم والرد عليه.
    * نظام رد آلي آمن في حالة عدم العثور على إجابة.

* **🩺 الأطباء القريبين:**
    * البحث عن أقرب الأطباء والمراكز الطبية باستخدام **GPS** الجهاز.
    * عرض الأطباء على الخريطة (يتطلب إضافة Google Maps SDK).

* **📚 مكتبة المحتوى (للأدمن):**
    * عرض المقالات والنصائح.
    * تصفح قاعدة بيانات الأدوية.
    * تصفح وصفات الأكل (مع فلترة بالفئة العمرية).
    * الاستماع للأصوات الهادئة (للمغص والنوم).

* **📔 تتبع إضافي:**
    * تسجيل السجلات اليومية (رضاعة، حفاض، نوم).
    * تسجيل يوميات الطفل (ذكريات + صور).

---

## 📸 لقطات شاشة (Screenshots)

(يُنصح بشدة بإضافة لقطات شاشة لواجهات التطبيق الرئيسية هنا)

`[... صورة شاشة تسجيل الدخول ...]`
`[... صورة شاشة الطفل الرئيسية ...]`
`[... صورة شاشة جدول التطعيمات ...]`
`[... صورة شاشة منحنى النمو ...]`
`[... صورة شاشة الكوميونيتي ...]`

---

## 🏛️ الهيكلة المعمارية (Architecture)

التطبيق مبني باتباع مبادئ **Clean Architecture** (المعمارية النظيفة) مع هيكلة **Feature-First** (المجلدات مقسمة حسب الميزة).